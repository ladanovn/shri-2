/**
 * Check interface block structure
 * @param {String} str
 * @returns {Array}
 */
const linter = str => {
};
if (window) {
    window.linter = linter;
}
else if (global) {
    global.linter = linter;
}
//# sourceMappingURL=linter.js.map